# Example packet

packets:
  telem:
    data:
      current:
        datatype: u32
        encoding: f32
        scaling: 10
        default: 2500
        min: 100
        max: 9000
        comment: This is a comment
        units: 0.1A per bit

      voltage:
        encoding: f32

      temperature:
        datatype: i16
